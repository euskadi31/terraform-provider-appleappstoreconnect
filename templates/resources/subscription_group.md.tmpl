---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

```hcl
data "appleappstoreconnect_app" "example" {
  bundle_id = "com.example.app"
}

resource "appleappstoreconnect_subscription_group" "premium" {
  app_id         = data.appleappstoreconnect_app.example.id
  reference_name = "Premium"
}
```

{{ .SchemaMarkdown | trimspace }}

## Import

Subscription groups can be imported using their ID:

```bash
terraform import appleappstoreconnect_subscription_group.premium XXXXXXXXXX
```
